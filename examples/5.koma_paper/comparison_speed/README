This script assumes that you have two NVIDIA GPUs and KomaNYU installed.

To run the script run as
	sh run_test.sh
If trying it in a dev version of KomaNYU you can specify the environment as an argument (cd'd to KomaNYU.jl/examples/5.koma_paper/comparison_speed/)
	sh run_test.sh --project=../../../